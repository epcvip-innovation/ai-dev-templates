# ──────────────────────────────────────────────────────────────
# allowed-commands.conf (Tier 3 — Strict)
#
# Minimal allow list. Everything else goes through Claude's
# permission prompt. Format: same as tier-2 (see that file).
# ──────────────────────────────────────────────────────────────

# --- Read-only utilities ---
Bash(ls *)
Bash(pwd)
Bash(wc *)
Bash(file *)
Bash(stat *)

# --- Git (read-only) ---
Bash(git status *)
Bash(git log *)
Bash(git diff *)
Bash(git branch *)
Bash(git show *)
Bash(git fetch *)
Bash(git stash list *)
Bash(git remote -v *)
Bash(git tag *)

# --- Git (standard workflow — still goes through settings allow) ---
Bash(git add *)
Bash(git commit *)

# --- Build/test (project-specific — uncomment what you need) ---
# Bash(npm run *)
# Bash(npm test *)
# Bash(pytest *)
# Bash(make *)
# Bash(cargo test *)
# Bash(go test *)

# --- Project-specific allowed scripts (CWD-independent) ---
# Bash(*/scripts/deploy.sh *)
# Bash(*/scripts/validate.sh *)
